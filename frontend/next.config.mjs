/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        SERVER_PROTOCOL: "http",
        SERVER_HOST: "localhost",
        SERVER_PORT: "3001",
      },
};

export default nextConfig;
